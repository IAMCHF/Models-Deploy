# 项目需求文档：内网模型批量部署工具

> **核心原则**：所有与 Hugging Face 相关的访问（下载权重、查阅示例代码）**优先使用国内镜像站 `https://hf-mirror.com`**，镜像站不可用时以官方 `huggingface.co` 作为兜底。所有生成的脚本和代码必须内置镜像站环境变量。

---

## 1. 项目目标

- 读取模型列表，自动生成**批量下载权重脚本**（基于国内镜像站），将权重下载到指定目录。
- 利用 **GitHub Actions** 构建稳定基础 Docker 镜像（含 PyTorch、CUDA、vLLM），产物供内网使用。
- 为每个模型自动生成**独立的 FastAPI 服务代码**、**虚拟环境脚本**、**启动脚本**、**测试代码**。
- 模型权重 ≥10GB 时自动标注，提醒内网容器扩容。
- 所有依赖安装启用缓存，避免重复下载。

---

## 2. 整体架构

```
外网: 模型列表 → Agent 生成批量下载脚本
  → 通过 HF 镜像站下载权重至 models/模型名/weights/
  → GitHub Actions 构建基础镜像 → 镜像产物供下载
  → 内网导入镜像 → 每个模型独立虚拟环境 + 服务
  → 启动服务 & 测试
```

---

## 3. 模型列表输入格式

Agent 解析用户提供的 **JSON / YAML / 纯文本列表**，每项包含：

- `model_id`：Hugging Face 模型标识，如 `mldi-lab/Kairos_23m`
- `notes`（可选）：备注

**命名转换规则（强制）**：

- 模型文件夹名 = `组织名-模型名`，**全部小写**，**仅含小写字母、数字、横杠（-）**，**无下划线、无小数点**，**长度 ≤50 字符**
- 示例：`mldi-lab/Kairos_23m` → `mldi-lab-kairos-23m`
- 示例：`google/timesfm-2.5-200m-transformers` → `google-timesfm-2-5-200m-transformers`

---

## 4. 基础镜像构建（GitHub Actions）

### 4.1 基础镜像选型分析

对 41 个模型的环境要求进行逐一排查，发现以下关键兼容性冲突：

| 冲突维度 | 冲突详情 | 涉及模型 |
|----------|----------|----------|
| **transformers 版本** | VibeVoice-ASR 要求 ≥5.3.0；MOSS-TTS 要求 ≥5.0.0；Timer-S1 锁定 ~=4.57.1；TIGER-DnR 锁定 ==4.47.1 | 4 个模型存在跨大版本冲突 |
| **PyTorch 版本** | MOSS-TTS 锁定 torch==2.9.1+cu128；TIGER-DnR 锁定 torch==2.5.1；TabPFN 要求 ≥2.1 | 3 个模型存在版本冲突 |
| **CUDA 版本** | MOSS-TTS 需 cu128（CUDA 12.8）；PaddlePaddle 支持 cu118/cu126 | 2 类模型有特殊 CUDA 需求 |
| **Python 版本** | MOSS-TTS 需 Python 3.12；Kronos 需 ≥3.10；TabPFN 需 ≥3.9 | MOSS-TTS 独有 3.12 要求 |
| **推理框架** | 大多数用 transformers/PyTorch；PaddlePaddle 模型独立；VideoPrism 用 JAX；CodeFormulaV2 用 Docling+ONNX | 4 类模型框架异构 |

**结论**：不存在单一基础镜像能满足全部 41 个模型的直接运行。采用**基础镜像 + 每模型独立 venv 覆盖**策略：基础镜像覆盖 ~30 个模型的默认需求，剩余 ~11 个模型通过各自 `create_env.sh` 在 venv 中覆盖版本。

### 4.2 Dockerfile 要求

- 基础镜像：`nvidia/cuda:12.4.1-cudnn-runtime-ubuntu22.04`
- 选择理由：
  - CUDA 12.4 是 PyTorch 2.1~2.5 和 ONNX Runtime GPU 广泛支持的版本
  - MOSS-TTS 需 cu128，但 torch cu128 wheel 自带 CUDA 12.8 运行时库，可在 12.4 基础上运行（宿主机驱动 ≥550 即可）
  - PaddlePaddle 自带 CUDA 库，不依赖基础镜像 CUDA 版本
  - 包含 cuDNN，大多数模型无需额外安装
- 预装（基础层，所有模型共享）：
  - **Python 双版本**：Python 3.11（默认 `python3`）+ Python 3.12（通过 deadsnakes PPA 安装，`python3.12`）
  - PyTorch 2.4.0 + torchaudio 2.4.0 + torchvision 0.19.0（cu124）——仅安装在 Python 3.11 下
  - transformers 4.52（最新稳定 4.x，覆盖 granite-speech/gte-modernbert 等要求 ≥4.48~4.52 的模型）——仅安装在 Python 3.11 下
  - vLLM（最新稳定版）——仅安装在 Python 3.11 下
  - onnxruntime-gpu
  - huggingface_hub, accelerate, safetensors, sentencepiece
  - soundfile, librosa, numpy, Pillow
  - 工具：`git`, `wget`, `curl`, `vim`, `ffmpeg`, `software-properties-common`
- 工作目录 `/workspace`
- 双版本说明：
  - `python3` / `pip3` 默认指向 3.11，30 个通用模型直接使用
  - `python3.12` 可用，MOSS-TTS 等需 3.12 的模型用 `python3.12 -m venv venv` 创建虚拟环境，在 venv 内安装所需 torch/transformers

### 4.3 需 venv 覆盖的模型（11个）

以下模型在 `create_env.sh` 中需覆盖基础镜像的默认版本：

| 模型 | 覆盖项 | 原因 |
|------|--------|------|
| OpenMOSS-Team/MOSS-TTS-Local-Transformer-v1.5 | torch==2.9.1+cu128, torchaudio==2.9.1+cu128, transformers>=5.0.0, python3.12 -m venv | 需最新 torch+CUDA 12.8，Python 3.12（基础镜像已预装） |
| OpenMOSS-Team/MOSS-VoiceGenerator | 同上 | 与 MOSS-TTS 共用环境 |
| microsoft/VibeVoice-ASR-HF | transformers>=5.3.0 | 需 transformers 5.x |
| bytedance-research/Timer-S1 | transformers~=4.57.1, accelerate | 锁定特定 transformers 版本 |
| JusperLee/TIGER-DnR | torch==2.5.1, torchaudio==2.5.1, torchvision==0.20.1, transformers==4.47.1, pytorch-lightning==2.0.2, triton==3.1.0 | 全套版本锁定 |
| PaddlePaddle/PP-DocBlockLayout | paddlepaddle-gpu==3.0.0, paddleocr | 需 PaddlePaddle GPU（自带 CUDA 12.6 库） |
| PaddlePaddle/PP-DocLayout_plus-L | 同上 | 同上 |
| PaddlePaddle/PP-Chart2Table | paddlepaddle-gpu==3.0.0, paddlex, paddlex[multimodal] | 需 PaddlePaddle + PaddleX |
| google/videoprism-lvt-base-f16r288 | jax, flax, SentencePiece | 需 JAX 框架而非 PyTorch |
| docling-project/CodeFormulaV2 | docling, onnxruntime | 需 Docling 框架 + ONNX 推理 |
| YuchenShen/FoMo-0D | pytorch-lightning, hydra, omegaconf, wandb | 需额外框架 + 本地仓库代码 |

> 剩余 30 个模型使用基础镜像默认环境即可，仅需在 `requirements.txt` 中补充模型特有依赖（如 `timm`, `deepguard`, `chronos-forecasting` 等）。

### 4.4 GitHub Actions 构建策略

- **禁止换源**：GitHub Actions 构建镜像时使用官方 PyPI 源（`pypi.org`）和官方 PyTorch 源（`download.pytorch.org`），禁止配置国内镜像源。GitHub Actions 运行在海外网络，访问官方源速度更快，换源反而降低构建速度
- 构建时启用 `--mount=type=cache,target=/root/.cache/pip` 缓存 PyPI 包
- 最终生成镜像压缩包（`.tar.gz`）作为 Actions artifact 供下载
- 国内镜像源换源仅在内网本地执行 `create_env.sh` 时使用（见第 7.3 节）

### 4.5 宿主机 GPU 驱动要求

- NVIDIA 驱动版本 ≥ 550.54（支持 CUDA 12.4 及向下兼容 cu128 wheel）
- 安装 NVIDIA Container Toolkit，使容器可访问 GPU

---

## 5. 项目目录结构

```
项目根目录/
├── models/
│   └── {模型文件夹名}/
│       ├── fastapi.py           # 服务主程序（端口8080）
│       ├── test/
│       │   ├── test.py          # 测试脚本
│       │   └── test_data        # 测试数据
│       ├── weights/             # 存放模型权重
│       ├── create_env.sh        # 创建虚拟环境（含换源）
│       ├── start.sh             # 启动服务
│       └── requirements.txt     # 模型特有依赖
├── download_weights.py          # 批量下载脚本（镜像站优先+官方兜底）
├── set_env.sh                   # 全局环境变量（镜像站优先）
├── Dockerfile
├── .github/workflows/build.yml
└── README.md
```

---

## 6. 批量下载权重脚本（`download_weights.py`）

**镜像站优先 + 官方兜底**：

- 脚本开头设定环境变量：
  ```python
  import os
  os.environ["HF_ENDPOINT"] = "https://hf-mirror.com"
  ```
- 使用 `huggingface_hub` 的 `snapshot_download`，自动读取该变量将请求指向镜像站
- 若镜像站不可用，自动 fallback 到官方 `huggingface.co`，并在日志中提示"镜像站不可用，已切换至官方源"
- 支持从环境变量 `HTTP_PROXY` / `HTTPS_PROXY` 读取代理（保留兼容性）
- 下载完成后计算 `weights/` 总大小，若 **≥10GB**，生成 `.large_model` 标记文件并汇总到 `large_models_list.txt`

---

## 7. 模型服务代码（`fastapi.py`）

### 7.1 接口规范

- FastAPI 服务，监听 `0.0.0.0:8080`
- `GET /health` → `{"status": "ok"}`
- `POST /predict` → 接收 JSON，`data` 字段为 **base64 编码**输入，响应 `result` 字段为 base64 编码输出

### 7.2 模型加载策略与代码获取（**关键约束**）

1. **代码撰写来源（强制）**：Agent **必须**访问镜像站或官方模型页面获取部署代码，**不允许默认生成通用加载代码**
   - 优先访问镜像站：`https://hf-mirror.com/{model_id}`
   - 镜像站无法访问时访问官方：`https://huggingface.co/{model_id}`
   - 解析页面中的 "Use in Transformers"、"Use in vLLM"、"How to use" 等代码片段，适配到 `fastapi.py`
2. **部署代码缺失时（强制交互）**：若镜像站和官方均无示例代码，Agent **必须暂停并汇总所有缺失模型列表询问用户**，禁止自行使用通用模板生成代码后再提醒
   - 汇总格式：列出模型名、已查找的页面、缺失原因
   - 等待用户确认后方可继续
3. **环境变量保障**：`fastapi.py` 中设定 `os.environ["HF_ENDPOINT"] = "https://hf-mirror.com"`，确保模型加载时若需下载额外配置或 tokenizer 优先走镜像站
4. **加载优先级**：`transformers` 加载 > `vLLM` 加载

### 7.3 依赖管理

- `requirements.txt` 仅含模型特有依赖（如 `accelerate`, `sentencepiece`, `timm` 等），基础依赖（torch, vllm, transformers）不在其中
- 对于第 4.3 节中需 venv 覆盖的 11 个模型，`create_env.sh` 必须先卸载基础镜像默认版本再安装指定版本（如 `pip install torch==2.9.1+cu128 --index-url https://download.pytorch.org/whl/cu128`）
- 对于需 Python 3.12 的模型（MOSS-TTS 系列），`create_env.sh` 直接用 `python3.12 -m venv venv` 创建虚拟环境（基础镜像已预装 Python 3.12），无需额外安装
- `create_env.sh` 执行步骤（仅在内网本地执行，非 GitHub Actions）：
  1. **换源**：测试清华、阿里、中科大源速度，选择最快的一个写入 `pip.conf`
  2. 创建虚拟环境 `venv`（默认 `python3 -m venv venv` 即 3.11；需 3.12 的模型用 `python3.12 -m venv venv`）
  3. 启用缓存：`export PIP_CACHE_DIR=$(pwd)/../.pip_cache`
  4. 如需覆盖基础版本，先 `pip uninstall` 旧版本
  5. 安装 `requirements.txt`
  6. 记录环境到 `env_info.txt`

### 7.4 启动脚本（`start.sh`）

```bash
#!/bin/bash
source ./venv/bin/activate
python fastapi.py
```

---

## 8. 测试规范（`test/test.py`）

- 检查服务是否运行在 `localhost:8080`
- 测试 `/health` 断言 `status=ok`
- 测试 `/predict`：读取 `test_data` 中的样例文件，base64 编码后发送，验证返回非空
- 测试数据生成策略：优先从镜像站模型页面的 "Files" 中下载官方样例（`https://hf-mirror.com/{model_id}/tree/main`），镜像站不可用时从官方下载（`https://huggingface.co/{model_id}/tree/main`）；均无则根据模型模态生成随机数据（文本/随机噪声图）

---

## 9. 全局镜像站配置（`set_env.sh`）

```bash
#!/bin/bash
export HF_ENDPOINT="https://hf-mirror.com"
export PIP_INDEX_URL="https://pypi.tuna.tsinghua.edu.cn/simple"
```

- `download_weights.py` 和 `create_env.sh` 必须主动 `source` 该文件或显式设定相同变量，确保网络请求优先走镜像站，镜像站不可用时自动 fallback 到官方源

---

## 10. 大模型标注（≥10GB）

- 下载脚本判断 `weights/` 大小，≥10GB 则在模型根目录创建 `.large_model` 文件
- 根目录生成 `large_models_list.txt`，并在 README 中提示内网容器需扩容存储

---

## 11. 交付物清单

1. **根目录**：`download_weights.py`、`set_env.sh`、`README.md`、`deploy_all.sh`（可选）、`.github/workflows/build.yml`、`Dockerfile`
2. **每个模型文件夹**：`fastapi.py`、`test/test.py` + 测试数据、`create_env.sh`、`start.sh`、`requirements.txt`

---

## 12. Agent 硬性约束

1. **镜像站优先、官方兜底**：所有下载、加载、代码查阅逻辑优先使用 `HF_ENDPOINT=https://hf-mirror.com`，镜像站不可用时 fallback 到 `huggingface.co`。最终代码中环境变量默认指向镜像站，同时保留 fallback 逻辑
2. **禁止默认生成通用加载代码**：Agent 必须访问镜像站或官方模型页面获取真实部署代码。若镜像站和官方均无示例代码，Agent **必须暂停并汇总所有缺失模型列表询问用户**，禁止自行使用 `AutoModel`/`pipeline` 等通用模板生成代码后提醒用户核对
3. 所有 Shell/Python 脚本必须包含错误处理（`set -e` / try-except），在镜像站和官方均不可用时给出清晰中文提示
4. **PyPI 源分场景**：GitHub Actions 构建镜像时使用官方 PyPI 源，禁止换源（海外网络官方源更快）；内网本地执行 `create_env.sh` 时换源至国内 PyPI 镜像（如清华源），加速构建
